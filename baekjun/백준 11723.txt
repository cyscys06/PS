백준 11723
내가 생각한 풀이:
모든 연산에 대해 함수를 만들고
실제 벡터에 연산을 적용해서 값이 그대로 들어가게 한다
-> 1 <= x <= 20인 x라는 값 자체가 벡터 안에 들어간다고 생각


실제 풀이: 비트마스킹 이용
배열 크기를 20만 설정하고,
각 인덱스는 1 <= x <= 20인 수들을 의미함
인덱스의 값은 해당 숫자의 유무를 나타냄(1이면 숫자 있음, 0이면 숫자 없음)
-> 각 연산은 배열 안의 숫자를 0 또는 1로만 바꿔주는 역할 함
-> 시간 단축 가능


