기능 추가 목록
1. 1개 이상의 음악을 사용해서 재생목록을 생성하는 기능
2. 재생 목록에 음악을 추가/삭제하는 기능
3. 음악 순환 기능(음악을 순차적으로 재생할 수 있어야 하고, 마지막 음악이 끝나면 다시 첫 번째 음악이 나오도록 해야 함)


개발 순서
1. 음악, 재생목록에 대한 데이터를 담을 데이터 구조 생성
2. 데이터 구조에 데이터 추가/삭제 기능 추가
3. 사용자 정의 이터레이터 추가(컨테이너를 순회하는 이터레이터)
4. next, previous, printall, insert, remove같은 함수 작성







- 후위증가연산자에 매개변수 int 넣는건 단순 구분을 위한것
- 선위증가연산자는 증가한 값을 참조로 바로 반환하지만, 
후위증가연산자는 증가하기 이전 값을 임시 객체에 복사해놓고, 실제 필드값은 증가시킨 후, 
이전 값을 복사해놓은 임시 객체를 대신 반환함
-> 이번 행에서는 후위증가연산자가 반환한 임시객체 값(이전값)을 사용하지만,
다음 행에서는 실제로 증가한 필드 값을 사용하는 것